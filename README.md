The app will include full CRUD (Create, Read, Update, Delete) operations and work well on both mobile and desktop devices.
Features:

    Add new tasks
    Edit existing tasks
    Mark tasks as completed
    Remove completed tasks
    Responsive design
